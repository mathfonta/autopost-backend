# Story 2.5 — Agente Onboarding (Claude Sonnet)

**Status:** Done  
**Epic:** 2 — Agentes de IA  
**Story:** 2.5  
**Estimativa:** 5 pontos  
**Depende de:** 1.2

---

## Descrição

Como novo cliente do AutoPost, quero passar por um fluxo de onboarding guiado por IA que coleta informações sobre minha empresa e cria meu `brand_profile` automaticamente — para que os agentes de copywriting e design usem meu tom de voz e identidade visual desde o primeiro post.

---

## Critérios de Aceitação

- [x] AC1: Endpoint `POST /onboarding/start` inicia sessão de onboarding para o cliente autenticado
- [x] AC2: Claude Sonnet conduz conversa estruturada coletando: nome da empresa, segmento, cidade, tom de voz, cores, redes sociais ativas
- [x] AC3: Ao final, `brand_profile` do cliente é preenchido com os dados coletados e salvo no banco
- [x] AC4: Fluxo A (tem Instagram): coleta `has_instagram: true` no brand_profile
- [x] AC5: Fluxo B (sem histórico): coleta dados básicos e cria perfil do zero
- [x] AC6: Onboarding pode ser retomado se interrompido (status salvo em Redis com TTL 24h)
- [x] AC7: Testes com mock do Anthropic SDK

---

## Tasks

- [x] Criar `app/agents/onboarding.py` — `start_session()` + `process_message()` com Claude Sonnet
- [x] Criar `app/api/onboarding.py` — endpoints: `POST /onboarding/start`, `POST /onboarding/message`, `GET /onboarding/status`
- [x] Implementar sessão Redis com TTL 24h (`app/core/redis_client.py`)
- [x] Claude emite marcador `PROFILE_COMPLETE` + JSON ao concluir — extraído com regex
- [x] Atualizar `Client.brand_profile` no banco quando onboarding conclui
- [x] Registrar router em `app/main.py`
- [x] Criar `tests/test_onboarding.py`

---

## Dev Notes

- Claude Sonnet 4.6 (`claude-sonnet-4-6`) para conversa de onboarding
- Sessão Redis: chave `onboarding:{client_id}`, JSON serializado, TTL 24h
- Marcador `PROFILE_COMPLETE` + JSON é extraído com `re.search` — removido da resposta final ao usuário
- Sessão expirada → reinicia automaticamente (não retorna erro)
- Campos obrigatórios: company_name, segment, city, tone
- Campos opcionais: primary_color, has_instagram
- `app/core/redis_client.py` também será usado no Epic 3 (WebSocket sessions)

---

## Escopo

**IN:** Coleta de dados da empresa, criação do brand_profile, sessão conversacional  
**OUT:** Upload de logo (Epic 3), conexão OAuth Meta (Story 2.4), tutoriais em vídeo

---

## File List

- `app/agents/onboarding.py` (criado)
- `app/api/onboarding.py` (criado)
- `app/core/redis_client.py` (criado — compartilhado com Story 2.3)
- `app/main.py` (modificado — onboarding router registrado)
- `tests/test_onboarding.py` (criado)

---

## Change Log

| Data | Autor | O que mudou |
|------|-------|-------------|
| 2026-04-13 | @sm | Story criada |
| 2026-04-13 | @dev | Implementada — 13 testes, 77 passando no total |
