# Story 2.2 — Agente Copywriter (Claude Sonnet)

**Status:** Done  
**Epic:** 2 — Agentes de IA  
**Story:** 2.2  
**Estimativa:** 3 pontos  
**Depende de:** 2.1

---

## Descrição

Como AutoPost, preciso que o Agente Copywriter use Claude Sonnet para gerar legenda, hashtags e CTA adaptados ao perfil de marca do cliente — substituindo o stub atual em `generate_copy`.

---

## Critérios de Aceitação

- [x] AC1: Task `generate_copy` chama Claude Sonnet com `analysis_result` + `brand_profile` e retorna copy estruturado
- [x] AC2: `copy_result` contém: `caption` (≤2200 chars), `hashtags` (lista), `cta`, `suggested_time` (horário sugerido HH:MM)
- [x] AC3: Caption nunca inventa dados — prompt instrui Claude a usar apenas informações presentes na análise
- [x] AC4: Tom de voz adaptado ao segmento do cliente (`brand_profile.segment`, `brand_profile.tone`)
- [x] AC5: Hashtags incluem: nicho do segmento + localização (se disponível) + hashtags gerais Instagram
- [x] AC6: Claude Sonnet — modelo `claude-sonnet-4-6` para qualidade máxima de copy
- [x] AC7: Testes com mock da API Anthropic

---

## Tasks

- [x] Criar `app/agents/copywriter.py` — função `generate_copy_with_ai(analysis_result, brand_profile)`
- [x] Criar prompt com instruções de tom, formato e restrições (sem invenção de dados)
- [x] Atualizar `app/tasks/pipeline.py` — substituir stub de `generate_copy`
- [x] Criar `tests/test_copywriter_agent.py`

---

## Dev Notes

- Claude Sonnet 4.6 (`claude-sonnet-4-6`) para geração de copy — melhor qualidade
- Passar `analysis_result.description` + `brand_profile` no prompt
- Validar tamanho da caption antes de salvar (truncar se > 2200 chars)
- JSON mode: pedir resposta em JSON estruturado com campos fixos
- Hashtags normalizadas: lstrip("#"), lower(), replace(" ", "")
- Horários de pico por segmento em `_DEFAULT_TIMES` como fallback
- `test_pipeline.py` atualizado: testes de `generate_copy` agora mocam `_get_request_with_client` e `generate_copy_with_ai`

---

## Escopo

**IN:** Geração de legenda, hashtags e CTA via IA  
**OUT:** Aprendizado de aprovações/rejeições (v1.1), múltiplas variações de copy (v1.1)

---

## File List

- `app/agents/copywriter.py` (criado)
- `app/tasks/pipeline.py` (modificado — stub substituído por IA real)
- `tests/test_copywriter_agent.py` (criado)
- `tests/test_pipeline.py` (modificado — mocks atualizados para generate_copy)

---

## Change Log

| Data | Autor | O que mudou |
|------|-------|-------------|
| 2026-04-13 | @sm | Story criada |
| 2026-04-13 | @dev | Implementada — 10 testes, 55 passando no total |
